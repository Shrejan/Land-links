{
    "version": 2,
    "builds": [
      {
        "src": "claint/src/**/*",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "claint/build"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/index.html"
      }
    ]
  }